# 🚀 起動方法（start.sh を使う）

 #① ターミナルを開く

 #② stock-price フォルダへ移動

```bash
  cd ~/projects/stock-price
 ```

 #③ 起動（←これだけ）

```bash
   ./start.sh
```

 #④プラウザで
 
 (リアルタイム株価情報)　[http://localhost:8000/index.html]

 #株価取得エラー :以下の通りしかし、<https://stockhome.pythonanywhere.com/>正常です

```txet
   1. 無料版の制限
外部へのアクセス制限
無料アカウントでは、外部 API へのアクセスは 一部サイトのみ許可されています。
Yahoo Finance (yfinance) は残念ながら無料プランからだとブロックされる可能性があります
その場合、yf.download() はエラーになるか、データが取得できません
CPU・時間制限
スクリプトを毎分・毎秒で実行するような「リアルタイム更新」は難しい
バックグラウンドで常時動かすには制限がかかります
2. 実際の挙動

あなたが前に見せてくれた表示：

📡 リアルタイム株価情報
日立製作所 : ¥4,811.00
ホンダ : ¥1,269.00
日本製鉄 : ¥585.00
サイバーステップ : ¥2,967.00

→ これは 成功している状態
→ つまり、その時点では yfinance がアクセス可能だったか、JSON がローカルで作られて反映された状態

しかし長期的には、無料プランだと 突然 API 取得が失敗 することがあります
3. 対策案
更新頻度を下げる
毎 1 秒 → 1 日 1 回（あるいは 10 分に 1 回）など
PythonAnywhere のスケジュールタスク機能を使う
ローカルでデータ作成 → JSON アップロード
ローカルで make_latest_json.py を走らせて最新データを生成
その JSON を PythonAnywhere にアップロード → フロントは JSON を読むだけ
有料版に切り替える
外部 API アクセスや長時間バックグラウンド実行が可能
無料制限の心配なし

💡 結論：

無料版でも「手動で更新 → 表示」なら OK ですが、リアルタイム更新は 失敗するリスクあり。
安全に動かしたいなら、JSON をローカル生成してアップロードする方が確実です。

希望なら、私は PythonAnywhere の無料プラン向けに最適化した安全な自動更新フロー を作って提示できます。
